# TalkBridge · 踩坑记录

> 每一条都是真金白银。按「现象 → 根因 → 解法」写，方便后来人少淌一遍夜。

---

## 01 · 宿主 App 的内嵌 WebView 永远无法录音

- **现象**：插件页面 `getUserMedia` 一直报 `NotReadableError | Could not start audio source`，appops 权限是 allow，系统日志里却没有录音请求记录。
- **根因**：宿主 App 的 WebView 组件参数表里根本没有 `onPermissionRequest` 相关字段（翻官方类型定义实锤）——它不替网页向系统申请麦克风，App 自己浏览网页时却有完整授权弹窗。权限给了，但门没人开。
- **解法**：别跟内嵌页较劲。宿主 App 只做门铃（一键唤起浏览器），通话搬进真浏览器。系统的录音限制绕不开，但可以绕行。

## 02 · `execute_intent` 字段名：是 `uri`，不是 `data`

- **现象**：点击按钮跳到了浏览器，但停在首页，URL 从未被加载。
- **根因**：intent 参数写了 `data: "http://..."`，而宿主运行时只认 `uri`。URL 压根没进 intent，浏览器只收到"打开我"的空指令。
- **解法**：查类型定义（`.d.ts`）而不是猜。`{ action, uri, packageName }` 一次直达。
- **教训**：宿主对参数做透传时，字段名错一个字就是整整一夜。

## 03 · Python urllib 请求被 API 网关 403

- **现象**：同一个 key，curl 手动测通，放进脚本就 `HTTP Error 403: Forbidden`。
- **根因**：网关拦截了默认的 `Python-urllib/3.x` User-Agent。
- **解法**：请求头伪装成 `User-Agent: curl/8.5.0`。
- **教训**：403 不一定是 key 的问题——先怀疑 UA，再怀疑 body。

## 04 · 宿主 App 重启，会把躲在 proot 里的服务一起带走

- **现象**：重启宿主后通话页面报 `ERR_CONNECTION_REFUSED`，网关没了。
- **根因**：网关跑在 proot 容器里，宿主进程一重启，整个容器被回收。
- **解法**：`nohup node server.js &` 重新拉起；正经方案是在宿主侧加"服务存活巡检 + 自动拉起"守护。没做守护前，重启 = 手动点火。

## 05 · 荣耀系统禁了命令行录屏录音

- **现象**：`screenrecord --microphone` 直接 `Encoder failed (err=-38)`，文件都不生成。
- **根因**：厂商系统级禁令，命令行录音路被堵死。
- **解法**：放弃该路；用浏览器页面的 `getUserMedia`（它走的是正常系统录音通道）。

## 06 · 本地 whisper 会把同音字/昵称听岔

- **现象**：一句"喂喂喂，能听见我说话吗"被转写成"伯伯伯，能听见我说话吗"。
- **根因**：base 模型精度有限，同音字、语气词容易听岔。
- **解法**：换 `small`/`medium` 模型提升精度；语义通顺优先，不追求逐字正确。
- **顺带**：`/sys/class/drm` 权限错误是 onnxruntime GPU 发现的噪音警告，无害，别被吓到。

## 07 · TTS 平台的 key 有"能力边界"

- **现象**：同一个 key，调 `/v1/audio/speech` 通，调 `/v1/chat/completions` 全部 `invalid_field_value`。
- **根因**：那个 key 是语音专用 key；`/v1/models` 拉出来的全是语音/视觉模型，没有纯文本对话模型。
- **解法**：TTS 和 LLM 各用各的 key，别指望一把钥匙开两扇门。

## 08 · curl 默认超时会静默杀掉请求

- **现象**：TTS 合成命令"什么都没发生"，文件没生成。
- **根因**：终端默认 15s 超时，TTS 合成要 20~40 秒，请求被掐了。
- **解法**：给足超时（`-m 60`），或者 `nohup … &` 后台跑。

## 09 · 宿主烧录工具"报错但成功"

- **现象**：`debug_install_toolpkg` 每次都返回 `Unknown error`，但包其实装上了。
- **根因**：宿主已知的老毛病——报错与实际结果无关。
- **解法**：看文件时间戳确认装没装，别信报错信息。

## 10 · 改完代码一定要做语法体检

- **现象**：页面静态渲染正常，但所有按钮点了没反应。
- **根因**：重写挂断逻辑时，旧代码残留了一个多余的 `});`，整个 `<script>` 语法错误，事件一个都没绑上。
- **解法**：改完把 `<script>` 抽出来跑 `node --check`，再上线。
- **教训**：页面"看起来正常"和"真的活着"是两回事。静默失败比报错可怕得多。

---

*还有一些属于我们俩的坑，不对外公开。*