# TalkBridge · PITFALLS 踩坑记录（工程版）

> 每条踩坑记录按「现象 → 根因 → 排查 → 解法 → 预防」五段式编写。
> 全部来自真机实测，报错文本为原始摘录。

---

## 01 · 宿主 App 的内嵌 WebView 无法录音

- **现象**：插件页 `getUserMedia` 持续报 `NotReadableError | Could not start audio source`。系统层面 appops `RECORD_AUDIO` 为 `allow`，但 `logcat` 中没有任何来自宿主进程的 `startInput`/`AudioRecord` 记录——请求根本没到达系统音频层。
- **根因**：宿主的插件 WebView 组件参数表中不存在 `onPermissionRequest` 相关字段（已翻官方类型定义 `.d.ts` 实锤）。网页请求麦克风时，宿主不替它向系统申请授权；而宿主自身浏览网页时走的是另一条完整授权流程，因此表现为"权限给了，门没人开"。
- **排查**：1) `appops get <pkg> RECORD_AUDIO` 确认系统授权；2) `logcat` 搜 `startInput`/`AudioRecord` 判断请求是否抵达音频层；3) 用浏览器直接打开同一页面对照（对照组通过 ⇒ 问题锁死宿主 WebView）。
- **解法**：不做修补，做绕行——宿主 App 只承担"一键唤起浏览器"（`execute_intent` + `uri`），录音与播放全部交给完整授权流程的真实浏览器。
- **预防**：立项时先验证"宿主内嵌页与宿主自带浏览器是否共用授权路径"，避免把时间花在不可修的问题上。

## 02 · `execute_intent` 字段名：`uri` 而非 `data`

- **现象**：点击按钮成功跳转浏览器，但停留在首页，URL 从未被加载。用户侧看到"进了浏览器但没进目标页"。
- **根因**：宿主运行时对 intent 参数做透传，但字段名不匹配——写入 `data`，宿主只认 `uri`。URL 被静默丢弃，intent 只剩"打开某某应用"的空指令。
- **排查**：在宿主脚本环境直接调用 `Tools.System.intent({...})` 并检查返回结构（`uri` 字段是否有值）；比对官方类型定义中的 `Intent` 类字段（`setData(uri)`）。
- **解法**：参数改为 `{ action, uri, packageName }`，一次直达。
- **预防**：宿主透传类 API，写前先查 `.d.ts` 类型定义，不凭直觉猜字段名。

## 03 · Python urllib 请求被 API 网关 403

- **现象**：同一 API Key，`curl` 手测通过（HTTP 200），脚本内调用稳定返回 `HTTP Error 403: Forbidden`。
- **根因**：网关拦截默认 `User-Agent: Python-urllib/3.x`（常见的反爬/反自动化策略）。
- **排查**：对比 curl 与脚本的请求头差异；单独替换 UA 头复测。
- **解法**：请求头显式携带 `User-Agent: curl/8.5.0`。
- **预防**：脚本类 HTTP 客户端默认带浏览器或 curl 风格 UA；403 先怀疑 UA，再怀疑 Key/权限。

## 04 · 宿主重启导致 proot 容器整体回收（服务全部消失）

- **现象**：宿主 App 重启后，通话页报 `net::ERR_CONNECTION_REFUSED`（127.0.0.1:18120 拒绝连接）；`curl /health` 无响应；`pgrep` 查不到网关与应答器任何进程。
- **根因**：服务运行在 proot 容器内，容器生命周期与宿主进程绑定。宿主进程一被重启/回收，整个容器被销毁，容器内所有 `nohup` 进程随之终止——不是单个服务崩溃，是**整层消失**。
- **排查**：1) `curl -s http://127.0.0.1:18120/health`；2) `pgrep -f 'node server.js'`；3) `pgrep -f 'auto_responder.py'`。三者全空 ⇒ proot 已被回收。
- **解法**：重新拉起（`nohup node server.js` + 带环境变量 `nohup python3 auto_responder.py`）。建议封装为一键脚本，带幂等自检：**存活则不拉起，死亡才拉起**（本项目：`start_local.sh`）。
- **预防**：将启动逻辑挂到宿主生命周期钩子（如 `registerAppLifecycleHook`）实现真·自动守护；或在宿主侧做定时探活。未做守护前，重启宿主 = 手动点火。
- **运维约定（使用者侧）**：宿主 App 每次重启后，proot 服务层都会整体回收——这是**预期内的日常运维动作，不是故障**。使用者只需让 AI（或手动执行 `start_local.sh`）重新拉起网关与应答器，全程约 30 秒。切勿误判为"网络 / 权限 / 配置"问题（见 05）。

## 05 · 把回环地址误判为网络问题

- **现象**：通话页连不上时，用户第一反应是"是不是要连 WiFi / 流量才能用"。
- **根因**：`127.0.0.1` 是回环地址（本机自通信），与蜂窝/WiFi **完全无关**。它连不上只意味着一件事：本机服务未运行。本项目真正依赖网络的是云端两步：LLM 生成回复、TTS 合成语音。
- **排查**：服务存活检查（见 04），而不是检查网络设置。
- **解法**：本地服务拉起即可；云端依赖单独验证各自的连通性。
- **预防**：文档中明确区分"本地服务依赖"与"云端依赖"的边界，避免误导排查方向。

## 06 · 厂商禁命令行录屏录音

- **现象**：`screenrecord --microphone` 直接失败：`Encoder failed (err=-38)`，无输出文件生成。
- **根因**：荣耀等厂商的系统级禁令——命令行录屏录音通道被关闭，与权限配置无关。
- **排查**：尝试该命令并观察编码器错误码；对比浏览器 `getUserMedia` 是否可用（它走正常系统录音通道）。
- **解法**：放弃命令行方案；录音统一走浏览器页面 `getUserMedia`。
- **预防**：评估"命令行采集音频"可行性前，先确认目标厂商系统是否开放该通道。

## 07 · 本地 whisper 对同音字/昵称听岔

- **现象**："喂喂喂，能听见我说话吗"被转写成"伯伯伯，能听见我说话吗"。
- **根因**：`base` 模型参数量有限，对同音字、语气词、亲昵称呼的区分度不足；夜间安静环境下偶发加重。
- **排查**：对同一音频换 `small`/`medium` 模型复测对比。
- **解法**：按精度需求升级模型；业务上以"语义通顺"为验收标准，不追求逐字正确。
- **预防**：对固定场景（如称呼词）可加后处理同音字表；顺带说明：`/sys/class/drm` 权限错误是 onnxruntime GPU 探测的无害噪音，非故障。

## 08 · TTS 平台 Key 存在能力边界

- **现象**：同一 Key 调 `/v1/audio/speech` 正常，调 `/v1/chat/completions` 一律返回 `invalid request / invalid_field_value`。
- **根因**：该 Key 为语音专用权限；`/v1/models` 列表可见其授权域内只有语音/视觉类模型，无纯文本对话模型。
- **排查**：拉取 `/v1/models` 查看 Key 可见模型集合；分别用两种接口做最小请求验证。
- **解法**：TTS 与 LLM 分开配置，各用各的 Key 与平台。
- **预防**：接入新平台时先看 `/v1/models` 和能力文档，别假设一把 Key 全平台通用。

## 09 · 命令默认超时静默中断长任务

- **现象**：TTS 合成命令"什么都没发生"，目标文件不存在，无任何报错输出。
- **根因**：终端默认超时约 15 秒，而 TTS 合成耗时 20~40 秒，请求被静默掐断。
- **排查**：`ls -la` 检查输出文件；用 `time curl ...` 测量实际耗时。
- **解法**：显式给足超时（`curl -m 60`），或 `nohup ... &` 丢后台再轮询产物。
- **预防**：所有云端合成/生成类调用，默认按 60 秒以上预算超时。

## 10 · 宿主烧录工具"报错但成功"

- **现象**：`debug_install_toolpkg` 每次返回 `Unknown error`，但包实际安装成功并可运行。
- **根因**：宿主已知缺陷——报错信息与实际安装结果无关联，属于"吓人但无害"类日志。
- **排查**：检查目标包文件时间戳是否更新、包列表是否出现新条目，两者为准，不信报错文本。
- **解法**：以文件系统与运行时事实为验收依据。
- **预防**：固定流程中加入"安装后校验"步骤（时间戳 + 列表）。

## 11 · 改完代码不做语法体检

- **现象**：页面静态渲染完全正常，但所有按钮点击无响应。
- **根因**：修改中残留多余语法符号（实测为多出一个 `});`），整个 `<script>` 解析失败，**全部事件绑定从未生效**——错误被静默吞掉，页面"看起来活着"。
- **排查**：浏览器控制台无报错时也要怀疑；将脚本抽离后跑静态语法检查。
- **解法**：改造前后都把 `<script>` 内容提取出来执行 `node --check` 再上线。
- **预防**：**改完必体检**。静默失败比显式报错危险得多。

## 12 · 拉起服务要全量，别只拉一半

- **现象**：网关 `/health` 返回 `ok`，但通话页面"回答中"永远等不到回复；`rec/` 目录里新录音堆积，无人转写。
- **根因**：网关与自动应答器是**两个独立进程**，手工拉起时只拉了网关（或只拉了其中一个），另一个长期处于死亡状态——"半个服务活着"比"全死"更难察觉。
- **排查**：`pgrep` 必须同时检查两个进程（`node server.js` + `auto_responder.py`），再对比应答器日志最后一行时间是否新鲜。
- **解法**：固定使用统一一键脚本（`start_local.sh`），带幂等自检：存活则不拉起、死亡才拉起，网关与应答器一次全量检查。
- **预防**：运维动作（尤其宿主重启后重接）不手工逐条拉起，一律走脚本——**拉起必自检，自检必全量**。

---

*还有一些属于我们俩的坑，不对外公开。*